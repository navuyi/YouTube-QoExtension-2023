import { Style } from '../../../../config/style'
import { fullViewport } from '../../../../config/style'

export const style: Style = {
  header: {
    fontSize: '24px',
    color: 'white',
    padding: 0,
    margin: 0,
  },
}
